# نشر المشروع على Streamlit Community Cloud

**النتيجة:** رابط عام دائم مثل `intervai-pro.streamlit.app` — يفتحه أي شخص من
أي مكان، بلا واي فاي مشترك وبلا بقاء حاسوبك مشغّلاً.

---

## لماذا هنا وليس Hugging Face

Hugging Face جعلت تشغيل Python مدفوعاً: المجاني صار **صفحات ثابتة فقط**
(HTML/JS). حتى خيار «موقع عام وكود خاص» صار يتطلب اشتراكاً.

**Streamlit Community Cloud** مجانية بالكامل، ومصنوعة لهذا الإطار تحديداً،
وتسمح بمستودع **خاص** — فيبقى الكود مخفياً والموقع عاماً.

---

## 1. مستودع GitHub

1. سجّلي في `github.com` إن لم يكن لديك حساب.
2. **New repository**:

```
Repository name : intervai-pro
Visibility      : Private        ← الكود يبقى مخفياً، والموقع يبقى عاماً
```

3. **لا** تضيفي README أو .gitignore من GitHub — موجودان هنا.

### رفع الملفات

**من المتصفح:** صفحة المستودع → **Add file** → **Upload files** → اسحبي
محتويات مجلد `deploy/` كاملاً → **Commit**.

> مجلد `.streamlit` قد لا يظهر في نافذة الاختيار لأنه مخفي. إن لم يظهر،
> أنشئيه من GitHub: **Add file → Create new file** واكتبي في خانة الاسم
> `.streamlit/config.toml` ثم الصقي محتواه.

**أو عبر Git:**

```bash
cd deploy
git init
git add .
git commit -m "IntervAI Pro"
git branch -M main
git remote add origin https://github.com/اسمك/intervai-pro.git
git push -u origin main
```

---

## 2. الربط بـ Streamlit Cloud

1. `share.streamlit.io` → **Sign in with GitHub** → اسمحي بالوصول.
2. **New app** → **Deploy from existing repo**:

```
Repository  : اسمك/intervai-pro
Branch      : main
Main file   : app.py
App URL     : intervai-pro          ← يصبح intervai-pro.streamlit.app
```

3. **قبل الضغط على Deploy** — افتحي **Advanced settings** → **Secrets**،
   والصقي:

```toml
FIREWORKS_API_KEY = "مفتاحك"

DB_HOST = "mysql-2d5b1b68-aishanea67-0706.h.aivencloud.com"
DB_PORT = "16357"
DB_NAME = "defaultdb"
DB_USER = "avnadmin"
DB_PASSWORD = "كلمة_سر_Aiven"
DB_SSL_CA = "ca.pem"
```

> صيغة TOML: كل قيمة بين علامتي اقتباس. تُحقن كمتغيّرات بيئة، فيقرأها الكود
> بـ `os.getenv` بلا تعديل — ولهذا أخرجنا الإعدادات إلى البيئة أصلاً.

4. **Deploy**.

---

## 3. أول تشغيل

يستغرق **5–10 دقائق**؛ `torch` كبير. تابعي السجل من الزاوية السفلية.

عند الفتح، تأكدي من الشريط الجانبي:

```
🟢 Database connected · avnadmin@mysql-...aivencloud.com:16357/defaultdb
```

الأخضر يعني أن كل شيء يُحفظ. الأحمر يعني خطأ في الأسرار.

---

## ⚠️ المخاطرة المعروفة: الذاكرة

الخطة المجانية تعطي **1 غيغابايت**. والتقدير:

```
torch (نسخة المعالج)   ~250 MB
نموذج MiniLM            ~90 MB
Streamlit وبقية الحزم  ~200 MB
                        ────────
                        ~550 MB من 1024
```

ضيّق لكنه ممكن. **إن ظهر خطأ نفاد ذاكرة** (`app has gone over its resource
limits`)، الحل: استبدال النموذج الدلالي المحلي بواجهة تضمين عبر API، وهو
يزيل `torch` كاملاً. قوليها لي ونعدّلها.

---

## أمور تُعرف مسبقاً

**النوم عند الخمول.** يُوقَف التطبيق بعد 12 ساعة بلا زيارة، وأول زائر بعدها
ينتظر نحو 30 ثانية. **افتحيه بنفسك قبل الدراسة ليستيقظ.**

**كل الردود تذهب إلى Aiven** — لا إلى phpMyAdmin المحلي:

```bash
python view_data.py            # ملخص
python view_data.py --export   # CSV للتحليل
```

**عند تعديل الكود:** انسخي الملف المعدّل إلى `deploy/` وارفعيه إلى GitHub.
Streamlit Cloud تعيد النشر تلقائياً خلال دقيقة.

**الأسرار لا تُرفع أبداً.** `.gitignore` يمنع `.env` و
`.streamlit/secrets.toml`. الموجود هنا `secrets.toml.example` وهو قالب فارغ.
